#arbitrary Argument
#Kwargs
#values in an kwargs for key items
#item inbuild function returns key pairs
def address_info(**kwargs):
    for key,value in kwargs.items():
        print(f"{key}: {value}")

address_info(street="Mungoni",
             city="Chuka",
             state="Ndagani",
             zip=2389)
def citty_info
   for key,value in kwargs.items():
       print
